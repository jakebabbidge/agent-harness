---
phase: 07-restore-hitl-for-container-model
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/hitl/question-store.ts
  - src/hitl/question-store.test.ts
  - src/cli/answer.ts
  - src/cli/answer.test.ts
  - src/cli/run.ts
autonomous: true
requirements: [EXEC-03]

must_haves:
  truths:
    - "CLI operator can answer a surfaced question using agent-harness answer and the agent resumes"
    - "Answer command writes answer.json to the correct worktree .harness/ path"
    - "QuestionStore supports worktree-based IPC directories"
    - "Run command logs the full answer command (with worktree path) when surfacing questions"
  artifacts:
    - path: "src/hitl/question-store.ts"
      provides: "QuestionStore with worktree-based path support"
      contains: "forWorktree"
    - path: "src/cli/answer.ts"
      provides: "Answer command adapted for worktree-based IPC"
      contains: "harness"
    - path: "src/cli/answer.test.ts"
      provides: "Tests for answer command with worktree paths"
      min_lines: 20
  key_links:
    - from: "src/cli/answer.ts"
      to: "src/hitl/question-store.ts"
      via: "QuestionStore.forWorktree or direct path"
      pattern: "question-store"
    - from: "src/cli/answer.ts"
      to: "<worktreePath>/.harness/answer.json"
      via: "submitAnswer writes answer.json"
      pattern: "answer\\.json"
    - from: "src/executor/executor.ts"
      to: "src/cli/answer.ts"
      via: "printed answer command includes worktree path or runId"
      pattern: "agent-harness answer"
---

<objective>
Adapt the answer CLI command and QuestionStore to write answer.json to the worktree .harness/ path, completing the HITL loop.

Purpose: Close the HITL loop so the CLI operator can answer agent questions and the agent resumes inside the container. This completes EXEC-03.
Output: Working answer command that writes to worktree-based IPC path, QuestionStore with worktree support, full HITL loop wired end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-restore-hitl-for-container-model/07-CONTEXT.md
@.planning/phases/07-restore-hitl-for-container-model/07-RESEARCH.md
@.planning/phases/07-restore-hitl-for-container-model/07-01-SUMMARY.md

@src/hitl/question-store.ts
@src/hitl/question-store.test.ts
@src/cli/answer.ts
@src/cli/run.ts
@src/executor/executor.ts
@src/types/index.ts

<interfaces>
<!-- Key types from Plan 01 that this plan builds on. -->

From src/types/index.ts:
```typescript
export interface QuestionRecord {
  runId: string;
  questions: QuestionItem[];
  timestamp: string;
}

export interface AnswerRecord {
  runId: string;
  answers: Record<string, string>;
  answeredAt: string;
}
```

From src/hitl/question-store.ts (current):
```typescript
export class QuestionStore {
  constructor(baseDir?: string);
  runDir(runId: string): string;
  async askAndWait(runId: string, input: { questions: unknown[] }): Promise<Record<string, string>>;
  async submitAnswer(runId: string, answers: Record<string, string>): Promise<void>;
  async getQuestion(runId: string): Promise<QuestionRecord | null>;
  async purgeRunDir(runId: string): Promise<void>;
}
```

From src/executor/executor.ts (after Plan 01):
```typescript
// TaskExecutor now logs: `agent-harness answer <runId> "<your answer>"` when question detected
// IPC path: <worktreePath>/.harness/question.json and answer.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worktree-path support to QuestionStore and adapt answer command</name>
  <files>src/hitl/question-store.ts, src/hitl/question-store.test.ts, src/cli/answer.ts, src/cli/answer.test.ts</files>
  <behavior>
    - Test: QuestionStore.forWorktree(worktreePath) creates a store where runDir maps to worktreePath/.harness/
    - Test: submitAnswer via forWorktree writes answer.json to worktreePath/.harness/answer.json
    - Test: getQuestion via forWorktree reads question.json from worktreePath/.harness/question.json
    - Test: answer command with --path flag writes answer.json to the specified worktree .harness/ dir
    - Test: answer command verifies question.json exists before writing answer
    - Test: answer command without --path falls back to temp dir behavior (backward compatible)
  </behavior>
  <action>
    1. Update `src/hitl/question-store.ts`:
       - Add static factory `forWorktree(worktreePath: string): QuestionStore` that creates a QuestionStore with baseDir = `path.join(worktreePath, '.harness')` and overrides `runDir()` to return baseDir directly (flat structure, no runId subdirectory). The simplest approach: add an optional `flat` boolean to the constructor. When flat=true, `runDir(runId)` returns `this.baseDir` (ignoring runId). `forWorktree` sets flat=true.
       - Keep existing constructor and behavior unchanged for backward compatibility.

    2. Update `src/hitl/question-store.test.ts`:
       - Add describe block for `forWorktree`:
         - Test: `forWorktree(tmpDir).runDir('any-id')` returns `<tmpDir>/.harness`
         - Test: `submitAnswer` via forWorktree writes answer.json to the flat .harness/ dir
         - Test: `getQuestion` via forWorktree reads question.json from the flat .harness/ dir

    3. Update `src/cli/answer.ts`:
       - Add optional `--path <worktree-path>` parameter to `answerCommand`
       - When `--path` is provided: use `QuestionStore.forWorktree(pathValue)` to create the store, pass empty string as runId (flat mode ignores it)
       - When `--path` is NOT provided: use existing `new QuestionStore()` with runId (backward compatible temp dir behavior)
       - Update the function signature: `answerCommand(runId: string, answerText: string, options?: { path?: string })`

    4. Create `src/cli/answer.test.ts`:
       - Test: with --path, answer command writes answer.json to `<path>/.harness/answer.json`
       - Test: with --path, answer command fails if no question.json exists at `<path>/.harness/question.json`
       - Test: with --path, answer command succeeds when question.json exists and writes correct AnswerRecord shape
       - Use temp directories and pre-write question.json for test setup

    5. Wire the --path option into the CLI program (check `src/cli/index.ts` or `bin/agent-harness.ts` for where commander options are defined). Add `.option('--path <worktree-path>', 'Path to worktree for container HITL')` to the answer subcommand.
  </action>
  <verify>
    <automated>npx vitest run src/hitl/question-store.test.ts src/cli/answer.test.ts --reporter=verbose</automated>
  </verify>
  <done>QuestionStore.forWorktree creates flat-path stores, answer command supports --path flag for worktree-based IPC, backward compatible without --path, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire answer command path into CLI and update run command question logging</name>
  <files>src/cli/run.ts, src/cli/index.ts</files>
  <action>
    1. Check `src/cli/index.ts` (or wherever commander program is defined) and ensure the `answer` subcommand accepts the `--path` option and passes it through to `answerCommand`.

    2. Update `src/executor/executor.ts` question surfacing log (from Plan 01) if needed:
       - Ensure the logged answer command includes the `--path` flag pointing to the worktree path so the operator can copy-paste it directly.
       - The log should read: `agent-harness answer <runId> --path <worktreePath> "<your answer>"`
       - This requires TaskExecutor to know worktreePath (it already receives it as a parameter).

    3. Update `src/cli/run.ts`:
       - In template mode: ensure runId is logged at the start so the operator knows which run to answer
       - In workflow mode: same -- the runId is already logged

    4. Run full test suite to verify nothing is broken: `npx vitest run --reporter=verbose`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Answer CLI accepts --path, executor logs full copy-pasteable answer command with --path, full test suite passes, HITL loop is complete: agent asks (question.json) -> host surfaces -> operator answers (answer.json via CLI) -> agent resumes</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- full test suite green
- QuestionStore.forWorktree creates flat-path store for worktree-based IPC
- answer CLI with --path writes answer.json to correct worktree location
- Executor question surfacing log includes --path flag for copy-paste convenience
- Backward compatibility: answer without --path still works via temp dir
</verification>

<success_criteria>
- CLI operator can run `agent-harness answer <runId> --path <worktree> "<answer>"` and answer.json appears at `<worktree>/.harness/answer.json`
- QuestionStore.forWorktree provides flat-path IPC directory support
- The answer command printed by TaskExecutor includes --path so operator can copy-paste
- Full test suite passes with no regressions
- HITL loop is complete: question.json (container) -> CLI surfacing (host) -> answer.json (CLI) -> agent resumes (container)
</success_criteria>

<output>
After completion, create `.planning/phases/07-restore-hitl-for-container-model/07-02-SUMMARY.md`
</output>
